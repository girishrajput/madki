<?php
/**
 * Consent bar slot — content cloned in by JS from the per-state templates.
 *
 * Always visible so the decision stays revocable rather than a one-way door.
 */

defined('ABSPATH') || exit;
?>
<div class="wfl-consent-bar" id="wfl-consent-bar" role="status" aria-live="polite"></div>
